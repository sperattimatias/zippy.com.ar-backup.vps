# --- Domain / TLS ---
TRAEFIK_DOMAIN=zippy.local
ACME_EMAIL=dev@zippy.local
CF_DNS_API_TOKEN=replace-me

# --- Runtime ---
NODE_ENV=production
LOG_LEVEL=info

# --- Postgres ---
POSTGRES_USER=zippy
POSTGRES_PASSWORD=zippy
POSTGRES_DB=zippy
DATABASE_URL=postgresql://zippy:zippy@postgres:5432/zippy?schema=public

# --- Redis ---
REDIS_URL=redis://redis:6379

# --- Auth ---
JWT_ACCESS_SECRET=change-me
JWT_ACCESS_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_DAYS=30
EMAIL_VERIFICATION_TTL_MIN=30

# --- MinIO ---
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio123
MINIO_BUCKET=driver-docs
